const path = require('path');
const PROJECT_DIR = path.resolve(__dirname, '../../');
const SRC = path.resolve(PROJECT_DIR, 'src');

module.exports = {
    images: path.resolve(PROJECT_DIR, 'static/images'),
    icons: path.resolve(PROJECT_DIR, 'static/icons'),

    //common components
};
